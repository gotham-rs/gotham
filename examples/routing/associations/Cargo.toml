[package]
name = "gotham_examples_routing_associations"
description = "An example of the Gotham web framework `Router` that shows how to associate multiple handlers to a single path."
version = "0.0.0"
publish = false

[dependencies]
gotham = { path = "../../../gotham" }
gotham_derive = { path = "../../../gotham_derive" }

hyper = "0.11"
futures = "~0.1.11"
mime = "0.3"
